{% extends 'base.html.twig' %}

{% block body %}
<style>


 /* estilos fondo  */
@keyframes animate {
    0%{
        transform: translateY(0) rotate(0deg);
        opacity: 1;
        border-radius: 0;
    }
    100%{
        transform: translateY(-1000px) rotate(720deg);
        opacity: 0;
        border-radius: 50%;
    }
}

.background {
    position: fixed;
    width: 100vw;
    height: 100vh;
    top: 0;
    left: 0;
    margin: 0;
    padding: 0;
    background: #d1d1d1;
    overflow: hidden;
}
.background li {
    position: absolute;
    display: block;
    list-style: none;
    width: 20px;
    height: 20px;
    background: rgba(255, 255, 255, 0.2);
    animation: animate 19s linear infinite;
    pointer-events: none; /* Esto permitirá que los eventos pasen a través de los elementos */
}




.background li:nth-child(0) {
    left: 5%;
    width: 179px;
    height: 179px;
    bottom: -179px;
    animation-delay: 1s;
}
.background li:nth-child(1) {
    left: 52%;
    width: 141px;
    height: 141px;
    bottom: -141px;
    animation-delay: 5s;
}
.background li:nth-child(2) {
    left: 52%;
    width: 109px;
    height: 109px;
    bottom: -109px;
    animation-delay: 7s;
}
.background li:nth-child(3) {
    left: 74%;
    width: 196px;
    height: 196px;
    bottom: -196px;
    animation-delay: 6s;
}
.background li:nth-child(4) {
    left: 7%;
    width: 155px;
    height: 155px;
    bottom: -155px;
    animation-delay: 11s;
}
.background li:nth-child(5) {
    left: 28%;
    width: 194px;
    height: 194px;
    bottom: -194px;
    animation-delay: 10s;
}
.background li:nth-child(6) {
    left: 0%;
    width: 170px;
    height: 170px;
    bottom: -170px;
    animation-delay: 16s;
}
.background li:nth-child(7) {
    left: 23%;
    width: 188px;
    height: 188px;
    bottom: -188px;
    animation-delay: 2s;
}
.background li:nth-child(8) {
    left: 40%;
    width: 141px;
    height: 141px;
    bottom: -141px;
    animation-delay: 25s;
}
.background li:nth-child(9) {
    left: 19%;
    width: 182px;
    height: 182px;
    bottom: -182px;
    animation-delay: 3s;
}


/* estilos titulo  */
body {
  background: #001224;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
}

a {
  font-size: 3em;
  font-weight: bold;
  font-family: sans-serif;
  text-decoration: none;
  color: transparent;
  background: url("{{ asset('.') }}") repeat-x #9E2343;
  background-position-x: 0%;
  background-position-y: 0%;
  background-size: auto;
  background-clip: border-box;
  -webkit-background-clip: text;
  background-clip: text;
  background-size: 200% 100%;
  background-position-x: 0;
  background-position-y: 2em;
  -webkit-transition: background-position-y 0.6s ease;
  transition: background-position-y 0.6s ease;
  -webkit-animation: waveAnimation 4s infinite linear;
  animation: waveAnimation 4s infinite linear;
  animation-play-state: running;
  -webkit-animation-play-state: paused;
  animation-play-state: paused;
}

a:hover {
  background-position-x: 0;
  background-position-y: 0;
  -webkit-animation-play-state: running;
  animation-play-state: running;
}

@keyframes waveAnimation{
  from {
    background-position-x: 0%;
  }
  to {
    background-position-x: 200%;
  }
}

/* Add container styles */
    .form-container {
        max-width: 80%; /* Adjust the maximum width as needed */
        margin: 20px auto;
        padding: 20px;
        background-color: transparent;
       
         max-height: 70%; /* Altura máxima del contenedor */
    overflow-y: auto; /* Agrega una barra de desplazamiento vertical cuando el contenido desborda */
    overflow-x: hidden;
        
    }

    /* Add form styles */
    form {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
    }

    label {
        margin-bottom: 10px;
    }

    input[type="checkbox"] {
        margin-right: 5px;
    }

    input[type="text"] {
        width: 100%;
        padding: 8px;
        margin-bottom: 10px;
        box-sizing: border-box;
    }

    button {
        padding: 10px;
        background-color: #9e2343;
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
    }
    .button2 {
   
  display: flex;
  align-items: center;
  position: relative;
   justify-content: center;
  border: #9e2343 solid 0.15em;
  border-radius: 0.25em;
  color: #ffffff; /* Cambiado a #9e2343 */
  background: #9e2343;
  font-size: 1em; /* Tamaño de fuente más pequeño */
  font-weight: 600;
  cursor: pointer;
  overflow: hidden;
  transition: border 300ms, color 300ms;
  user-select: none;

  width:100%;

  }

.pagination {
    margin-top: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.pagination button {
    background-color: #9e2343;
    color: #ffffff;
    border: none;
    padding: 8px 16px;
    cursor: pointer;
    border-radius: 4px;
    margin: 0 5px;
}

.pagination span {
    padding: 8px 16px;
    border-radius: 4px;
    background-color: #9e2343;
    color: #ffffff;
    margin: 0 5px;
}
 
  
</style>

<ul class="background"  >
   <li></li>
   <li></li>
   <li></li>
   <li></li>
   <li></li>
   <li></li>
   <li></li>
   <li></li>
   <li></li>
   <li></li>
     
    {% include 'components/navbar.html.twig' %}
    {% set id = app.request.attributes.get('id') %}

 <div class="form-container"  >
     
   
   <a href="#">Lista de datos </a><br><br>

  <div class="col-span-2 rounded-md">
     <ul>
        
        <li2 style="font-weight: bold;">Fecha de captura: {{ data.fecha }}</li2><br>
        <li2 style="font-weight: bold;">Area solicitante: {{ data.area_solicitante }}</li2><br>
        <li2 style="font-weight: bold;">Centro de trabajo: {{ data.centro_trabajo}}</li2><br>
        <li2 style="font-weight: bold;">Nombre Solicitante: {{ data.nombre_solicitante}}</li2><br>
        <li2 style="font-weight: bold;">Puesto Solicitante: {{ data.puesto_solicitante}}</li2><br>
        <li2 style="font-weight: bold;">Telefono y extensión: {{ data.telefono_ext}}</li2><br>
        <li2 style="font-weight: bold;">Tipo de trabajo: {{ data.tipo_trabajo}}</li2><br>
        <li2 style="font-weight: bold;">Descripción trabajo: {{ data.descripcion_trabajo}}</li2><br>
    </ul><br><br>

  </div>


    <div class="botones-container">
        <button class="boton" onclick="mostrarTabla('tabla1')">Visualizar levantamientos</button>
        <button class="boton" onclick="mostrarTabla('tabla2')">Ordenes de trabajo</button>
    </div>

<div id="tabla1" class="tabla-container" style="display: block;">
        
     <table class="table">
                <thead>
                    <tr>
                        <th>Categoria</th>
                        <th>Fecha de realizacion del levantamiento</th>
                        <th>Ver</th>
                        <!-- Agrega más columnas según tus necesidades -->
                    </tr>
                </thead>
                <tbody>
                    {% for levantamiento in levantamientos %}
                        <tr>
                            <td>{{ levantamiento.id}}</td>
                            <td>{{ levantamiento.fecha_levantamiento|date('d-m-Y') }}</td>
                            <td>
                                <a href="{{ path('generar_proyecto_pdf_inspeccion', {'id': levantamiento.id}) }}" class="button2" target="_blank">Ver levantamiento y materiales</a>
                            </td>

                        </tr>
                    {% endfor %}
                </tbody>
            </table>
    <button id="nueva-captura-button" onclick="location.href='{{ path('app_iniciar_inspeccion', {'id': id}) }}'">Generar levantamiento</button>
</div>

<div id="tabla2" class="tabla-container" style="display: none;">
    <table class="table">
                <thead>
                    <tr>
                        <th>Nombre</th>
                        <th>Fecha de orden</th>
                        <th>Ver</th>
                        <!-- Agrega más columnas según tus necesidades -->
                    </tr>
                </thead>
                <tbody>
                    {% for nota in aceptadas %}
                        <tr>
                            <td>{{ nota.nombre }}</td>
                            <td>{{ nota.fechaPeticion|date('d-m-Y') }}</td>
                            <td>
                                <a href="{{ path('proyecto_ver_nota', {'id': nota.id}) }}" class="button2">Ver Nota Completa</a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
    <button id="nueva-captura-button" onclick="location.href='{{ path('app_solicitante_crear_orden', {'id': data.id}) }}'">Generar orden nueva</button>
</div>

{% for flash_message in app.flashes('success') %}
        <script>
            setTimeout(function() {
                Swal.fire({
                    icon: 'success',
                    title: '¡Éxito!',
                    text: '{{ flash_message }}',
                    timer: 3000, // Duración en milisegundos
                    timerProgressBar: true, // Barra de progreso del temporizador
                    showConfirmButton: false // Ocultar botón de confirmación
                });
            }, 100); // Cambia el valor del retraso según sea necesario
        </script>
    {% endfor %}

<script>
    function mostrarTabla(tablaId) {
        // Ocultar todas las tablas
        document.querySelectorAll('.tabla-container').forEach(function(tabla) {
            tabla.style.display = 'none';
        });

        // Mostrar la tabla seleccionada
        document.getElementById(tablaId).style.display = 'block';
    }
</script>

  </div>
  
 </ul>

    
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function() {
            $('input[type="checkbox"]').change(function() {
                var checkbox = $(this);
                var hiddenField = checkbox.prev('input[type="hidden"]');
                hiddenField.val(checkbox.prop('checked') ? 1 : 0);
            });
        });
    </script>
{% endblock %}
