{% extends 'base.html.twig' %}

{% block title %}Material{% endblock %}

{% block body %}
    <h1>Material</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ material.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ material.nombre }}</td>
            </tr>
            <tr>
                <th>Unidad_medida</th>
                <td>{{ material.unidadMedida }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ material.precio }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_material_crud_index') }}">back to list</a>

    <a href="{{ path('app_material_crud_edit', {'id': material.id}) }}">edit</a>

    {{ include('material_crud/_delete_form.html.twig') }}
{% endblock %}
